import SenderWidget from "@/app/components/SenderWidget";
import React from "react";

const SenderPage = () => {
  return (
    <div className="flex justify-center items-center ">
      <SenderWidget />
    </div>
  );
};

export default SenderPage;
